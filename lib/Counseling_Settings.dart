import 'package:flutter/material.dart';

class Counseling_Settings extends StatelessWidget {
  const Counseling_Settings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
