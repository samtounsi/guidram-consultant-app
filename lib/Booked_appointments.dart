import 'package:flutter/material.dart';

class Booked_appointments extends StatelessWidget {
  const Booked_appointments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
